{
  "name": "myshortbiz",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n API,WEB -c green,cyan \"bash -lc 'cd server && . .venv/bin/activate && uvicorn main:app --reload --port 8000'\" \"bash -lc 'npm run dev:web'\"",
    "dev:web": "vite",
    "build": "vite build",
    "start": "node ./server/start.js || echo 'No node server to start'"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.0",
    "concurrently": "^9.0.0",
    "vite": "^5.4.21"
  },
  "description": "MyShortBIZ frontend + backend (fixed scripts)",
  "main": "eslint.config.js",
  "keywords": [],
  "author": "",
  "license": "ISC"
}